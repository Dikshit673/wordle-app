const StatsTab = () => {
  // placeholder — implement persistence to localStorage later
  return (
    <div className='text-sm text-slate-600'>
      Stats placeholder — implement localStorage persistence for wins/attempts
      later.
    </div>
  );
};

export default StatsTab;
